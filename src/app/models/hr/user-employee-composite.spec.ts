import { UserEmployeeComposite } from './user-employee-composite';

describe('UserEmployeeComposite', () => {
  it('should create an instance', () => {
    expect(new UserEmployeeComposite()).toBeTruthy();
  });
});
